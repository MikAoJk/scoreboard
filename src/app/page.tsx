import ScoreBoard from "@/components/ScoreBoard";

export default function Home() {
    return (
        <main className="flex items-center text:2xl md:text-6xl">
            <ScoreBoard/>
        </main>
    );
}
